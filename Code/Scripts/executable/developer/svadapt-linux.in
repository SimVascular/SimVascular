
# use this if you are using a license file
# instead of embedded keys
# run the executable
if [ -e $SIM_LICENSE_FILE ] ; then
$SIMVASCULAR_HOME/Bin/@ADAPTOR_EXE@ $*
else
echo "No license key for MeshSim found!!!  Exiting."
fi
